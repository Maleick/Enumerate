#!/usr/bin/env bash

declare -r AQUA=`which aquatone`
declare -r AWK=`which awk`
declare -r CAT=`which cat`
declare -r CME=`which cme`
declare -r CURL=`which curl`
declare -r DIRB=`which dirb`
declare -r MKDIR=`which mkdir`
declare -r MSF=`which msfconsole`
declare -r NIKTO=`which nikto`
declare -r NMAP=`which nmap`
declare -r RPC=`which rpcclient`
